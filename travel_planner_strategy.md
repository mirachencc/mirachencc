# travel-planner技能策略整理

## 一、技能核心策略（分步骤）

### **第一步：需求收集**
**一次性询问所有问题，不分多次问：**

```
你好！我来帮你规划一份省心又好玩的旅行行程 🗺️

请告诉我以下信息：

1. 目的地是哪里？（国家/城市，可以是大范围，例如"斯里兰卡"或"泰国北部"）
2. 出行天数？（例如：7天6夜）
3. 从哪里出发？（用于安排第一天交通参考）
4. 旅行风格偏好？（可多选）
   - 🏛️ 人文历史   🌿 自然风光   🏖️ 海滩度假
   - 🍜 美食探索   📸 网红打卡   🎯 体验项目（冲浪/safari等）
5. 行程节奏偏好？
   - 🐢 轻松型（每天2-3个地点，留白时间多）
   - 🚶 标准型（每天3-4个地点）
   - 🏃 特种兵型（每天5+个地点，把景点榨干）
6. 有没有特别想去的地方，或者一定要避开的？（选填）
```

**收到回答后，直接进入第二步，不需要再次确认。**

---

### **第二步：多源检索攻略信息**

#### **2.1 Google搜索（主要信息源）**
- **搜索模板**：`{目的地}+{天数}+行程+攻略+2025`
- **提取内容**：行程框架、景点信息、交通方式
- **命令示例**：
  ```bash
  openclaw browser open "https://www.google.com/search?q=日本+7天+行程+攻略+2025"
  openclaw browser evaluate --fn "提取函数"
  ```

#### **2.2 小红书搜索（辅助信息源）**
- **搜索模板**：`{目的地}+{天数}+行程`
- **提取策略**：
  - **有OCR时**：截图 → OCR识别 → 提取详细攻略
  - **无OCR时**：文本提取 → 获取标题和元数据
- **命令示例**：
  ```bash
  openclaw browser open "https://www.xiaohongshu.com/search_result?keyword=日本+7天+行程"
  ```

#### **2.3 OCR检测与使用**
```bash
# 自动检测OCR工具
if command -v tesseract &> /dev/null; then
    echo "✅ OCR工具可用，使用截图+OCR提取小红书内容"
    # 使用OCR提取详细内容
else
    echo "⚠️ OCR工具不可用，使用文本提取"
    # 仅提取文本内容
fi
```

---

### **第三步：Google Maps餐厅搜索**

#### **3.1 餐厅搜索**
- **搜索模板**：`{城市}+餐厅`
- **提取内容**：餐厅名称、评分、价格、用户评价
- **命令示例**：
  ```bash
  openclaw browser open "https://www.google.com/maps/search/东京+餐厅"
  ```

#### **3.2 景点搜索**
- **搜索模板**：`{城市}+景点`
- **提取内容**：景点信息、开放时间、门票
- **命令示例**：
  ```bash
  openclaw browser open "https://www.google.com/maps/search/东京+景点"
  ```

---

### **第四步：智能搜索与提取（迭代流程）**

#### **4.1 初步提取**
- 从Google搜索提取：城市、景点、交通
- 从小红书提取：预算、路线、热门城市

#### **4.2 深度搜索**
基于初步提取的信息进行针对性搜索：
1. **城市详情**：`{城市}+旅游攻略`
2. **景点详情**：`{景点}+开放时间+门票`
3. **交通详情**：`{城市}+交通方式`
4. **餐厅详情**：`{城市}+特色美食`

#### **4.3 信息验证**
- 交叉验证多个来源的信息
- 优先使用高评分、新近的信息
- 标记不确定的信息

---

### **第五步：信息整合与行程生成**

#### **5.1 信息整合**
- **Google搜索**：行程框架、景点列表、交通信息
- **小红书**：预算参考、真实体验、避坑指南
- **Google Maps**：餐厅推荐、位置信息、用户评分

#### **5.2 行程框架构建**
1. **确定路线**：基于小红书提取的热门路线
2. **分配天数**：根据用户偏好和景点密度
3. **安排交通**：考虑时间和成本
4. **插入餐厅**：基于Google Maps推荐

#### **5.3 每日详细安排**
- **上午**：主要景点（2-3个）
- **中午**：餐厅用餐
- **下午**：次要景点或自由活动
- **晚上**：晚餐和娱乐

---

### **第六步：输出格式与交付**

#### **6.1 输出格式**
- **Markdown表格**：清晰易读
- **JSON格式**：结构化数据
- **纯文本列表**：简洁明了
- **日历视图**：时间线展示

#### **6.2 交付内容**
1. **行程概览**：路线、天数、预算
2. **每日安排**：时间、地点、活动
3. **餐厅推荐**：名称、评分、价格
4. **交通信息**：方式、时间、费用
5. **实用贴士**：注意事项、省钱技巧
6. **备用方案**：天气应对、计划调整

#### **6.3 交付方式**
- **直接回复**：在聊天中发送
- **保存为文件**：导出为文本文件
- **发送到聊天工具**：集成到其他平台

---

## 二、OCR集成策略

### **OCR检测逻辑**
```bash
# 检测OCR工具
which tesseract && echo "OCR可用" || echo "OCR不可用"

# 自适应执行
if OCR可用且用户偏好小红书:
    使用OCR提取小红书详细攻略
elif OCR不可用:
    使用Google搜索为主，小红书标题参考为辅
else:
    使用混合策略，以Google搜索为核心
```

### **OCR提取流程**
1. **截图**：`openclaw browser screenshot --full-page`
2. **OCR识别**：使用Tesseract或EasyOCR
3. **内容分析**：提取预算、城市、路线等信息
4. **结果保存**：保存OCR提取的文本和分析结果

### **XiaohongshuOCR类功能**
- `take_screenshot()`：截图网页
- `ocr_recognize()`：OCR识别图片文字
- `analyze_xiaohongshu_content()`：分析小红书内容
- `process_xiaohongshu_search()`：完整处理流程

---

## 三、信息源策略选择

### **策略A：无OCR工具**
- **主要信息源**：Google搜索
- **辅助信息源**：Google Maps + 小红书标题参考
- **特点**：基础可靠，无需额外工具
- **适用场景**：快速规划，基础信息需求

### **策略B：有OCR工具**
- **主要信息源**：小红书（OCR提取）
- **辅助信息源**：Google搜索 + Google Maps
- **特点**：获取真实用户分享，信息更接地气
- **适用场景**：深度规划，追求真实体验

### **策略C：混合策略**
- **信息源组合**：小红书(OCR) + Google搜索 + Google Maps + 其他平台
- **特点**：最全面信息收集，交叉验证
- **适用场景**：重要旅行，追求最佳体验

---

## 四、自适应执行流程

### **1. 环境检测**
```bash
# 检测浏览器状态
openclaw browser status

# 检测OCR工具
which tesseract

# 检测网络连接
ping -c 1 google.com
```

### **2. 用户偏好询问**
```
请问你希望主要参考哪个平台的信息？
- A. 小红书（素人分享真实，但需要OCR工具）
- B. Google搜索（信息全面，无需额外工具）
- C. 混合使用（综合各平台优势）
```

### **3. 策略选择**
- **用户选A且OCR可用** → 使用策略B（小红书为主）
- **用户选A但OCR不可用** → 使用策略A（Google为主）
- **用户选B** → 使用策略A（Google为主）
- **用户选C** → 使用策略C（混合策略）
- **用户无偏好** → 根据环境自动选择

### **4. 执行优化**
- **网络慢时**：减少搜索次数，使用缓存
- **内容少时**：扩大搜索范围，使用更多关键词
- **提取失败时**：尝试替代方法，截图分析

---

## 五、技术限制与应对

### **限制1：小红书内容为图片形式**
- **问题**：`openclaw browser evaluate`无法提取图片文字
- **解决方案**：使用OCR工具识别截图

### **限制2：网页端限制**
- **问题**：小红书网页端只能查看搜索结果页
- **解决方案**：提取搜索结果页的标题和元数据

### **限制3：需要登录**
- **问题**：某些内容需要登录才能查看
- **解决方案**：建议用户提前登录，或使用公开信息

### **限制4：页面结构变化**
- **问题**：网站改版导致提取函数失效
- **解决方案**：定期更新提取函数，使用通用选择器

### **限制5：OCR准确率**
- **问题**：图片质量影响识别准确率
- **解决方案**：图片预处理，结合多个OCR工具

---

## 六、版本更新策略

### **v2.2 (2026-03-26)**
- **主要更新**：集成截图和OCR能力
- **新增功能**：支持小红书图片内容提取
- **优化内容**：自适应策略选择

### **v2.1 (2026-03-26)**
- **主要更新**：调整信息源策略
- **明确原则**：小红书仅作参考，主要依赖Google搜索
- **优化内容**：信息提取逻辑

### **v2.0 (2026-03-26)**
- **主要更新**：支持Google Maps实时餐厅搜索
- **新增功能**：增强内容提取功能
- **优化内容**：多源信息整合

### **v1.0 (初始版本)**
- **基础功能**：行程规划框架
- **核心流程**：需求收集 → 信息检索 → 行程生成

---

## 七、最佳实践建议

### **1. 预处理**
- 确保OpenClaw浏览器已启动
- 建议用户提前登录小红书
- 检查OCR工具安装状态

### **2. 执行优化**
- 一次性询问所有需求问题
- 根据环境自动选择最佳策略
- 优先使用可靠的信息源

### **3. 结果交付**
- 提供多种输出格式选择
- 包含实用贴士和备用方案
- 标记不确定的信息

### **4. 后续优化**
- 收集用户反馈改进策略
- 定期更新提取函数
- 建立常见目的地知识库

---

## 八、快速参考命令

### **浏览器命令**
```bash
# 启动浏览器
openclaw browser start

# 打开网页
openclaw browser open "URL"

# 截图
openclaw browser screenshot --full-page

# 执行JavaScript
openclaw browser evaluate --fn "function() { ... }"
```

### **OCR命令**
```bash
# 检查OCR工具
which tesseract

# 测试EasyOCR
python3 -c "import easyocr; print('可用')"

# 运行OCR测试
python3 /path/to/ocr_test.py
```

### **技能测试**
```bash
# 测试完整流程
python3 /path/to/test_full_workflow.py

# 测试OCR集成
python3 /path/to/test_skill_ocr.py
```

---
*整理时间：2026-03-26 16:22 (Asia/Shanghai)*